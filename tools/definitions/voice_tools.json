{
    "tools": [
        {
            "name": "balcon_tts",
            "description": "调用Balcon实现文本转语音",
            "parameters": {
                "type": "object",
                "properties": {
                    "text": {
                        "type": "string",
                        "description": "要合成语音的文本（必填）"
                    },
                    "save_to_file": {
                        "type": "string",
                        "description": "保存语音文件的路径（如\"output.wav\"，可选，不填则直接播放）"
                    },
                    "voice_name": {
                        "type": "string",
                        "description": "语音库名称（可选，不填则用默认语音）"
                    },
                    "speed": {
                        "type": "integer",
                        "description": "语速（1-200，默认100）"
                    }
                },
                "required": [
                    "text"
                ]
            }
        },
        {
            "name": "list_balcon_voices",
            "description": "调用Balcon列出所有可用的语音库（去重+过滤无效行）",
            "parameters": {
                "type": "object",
                "properties": {}
            }
        }
    ]
}